spring.application.name=Healthflow Backend
server.port=${PORT:8080}

# MySQL config
spring.datasource.url=jdbc:mysql://mainline.proxy.rlwy.net:55062/railway?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=tMkhNQzLyUsEfkOmLkhgCvcsNkfCjDoe
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA + Hibernate
# "Keep existing tables and only add new columns if needed."mvn clean package
spring.jpa.hibernate.ddl-auto=none
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# forces Hibernate to retrieve metadata explicitly
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=true

# HikariCP tuning
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.auto-commit=true
spring.datasource.hikari.transaction-isolation=TRANSACTION_READ_COMMITTED